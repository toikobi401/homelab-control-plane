# ---------------------------------------------------------------------------
# Secrets — xem CONTEXT.md §6.5. KHÔNG BAO GIỜ commit các file này.
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
appsettings.Local.json
appsettings.*.Local.json
secrets.json
*.tailscale-authkey
rclone.conf
*.pfx
*.key
*.pem
!*.example.pem

# Cơ sở dữ liệu — chứa hash mật khẩu, phiên đăng nhập, thông tin xác thực đã mã hoá
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# ---------------------------------------------------------------------------
# .NET
# ---------------------------------------------------------------------------
bin/
obj/
[Dd]ebug/
[Rr]elease/
x64/
x86/
*.user
*.suo
*.userprefs
.vs/
artifacts/
publish/
*.nupkg
TestResults/
coverage*.json
coverage*.xml
*.coverage

# ---------------------------------------------------------------------------
# Node / frontend
# ---------------------------------------------------------------------------
node_modules/
dist/
dist-ssr/
.vite/
*.local
.eslintcache
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------------------------------------------------------------------------
# IDE
# Track: code style + inspection profiles dùng chung. Bỏ: state cá nhân.
# ---------------------------------------------------------------------------
.idea/*
!.idea/codeStyles/
!.idea/inspectionProfiles/
.idea/codeStyles/codeStyleConfig.xml
*.iml
.vscode/*
!.vscode/extensions.json
!.vscode/settings.shared.json

# ---------------------------------------------------------------------------
# Hệ điều hành
# ---------------------------------------------------------------------------
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk
.DS_Store

# ---------------------------------------------------------------------------
# Khác
# ---------------------------------------------------------------------------
*.log
*.tmp
*.bak
.claude/settings.local.json
.playwright-mcp/

# Nhật ký làm việc nội bộ — chứa ghi chú quá trình và chi tiết hạ tầng riêng.
# Giữ ở máy, không đưa lên bản public.
PROGRESS.md

# File tĩnh của frontend, sinh ra bởi 'npm run build' rồi chép vào đây.
# Là artifact build, không phải mã nguồn.
backend/Hub.Api/wwwroot/

# ---------------------------------------------------------------------------
# Python — script quản lý thiết bị trong scripts/
# ---------------------------------------------------------------------------
__pycache__/
*.pyc
.venv/
